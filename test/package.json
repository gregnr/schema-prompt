{
  "name": "test",
  "scripts": {
    "prompt-config": "schema-prompt"
  },
  "schema-prompt": {
    "files": [
      "config/*.schema.json"
    ]
  },
  "dependencies": {
    "schema-prompt": "../"
  }
}
