{
  "name": "test",
  "scripts": {
    "prompt-config": "schema-prompt"
  },
  "schema-prompt": {
    "type": "yaml",
    "files": [
      "config/*.schema.json"
    ]
  },
  "dependencies": {
    "schema-prompt": "../"
  }
}
